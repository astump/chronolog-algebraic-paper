% [TODO]: make sure use of roll1/roll2 and roll/unroll is consistent

%Version 3.1 December 2024
% See section 11 of the User Manual for version history
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%                                                                 %%
%% Please do not use \input{...} to include other tex files.       %%
%% Submit your LaTeX manuscript as one .tex document.              %%
%%                                                                 %%
%% All additional figures and files should be attached             %%
%% separately and not embedded in the \TeX\ document itself.       %%
%%                                                                 %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%\documentclass[referee,sn-basic]{sn-jnl}% referee option is meant for double line spacing

%%=======================================================%%
%% to print line numbers in the margin use lineno option %%
%%=======================================================%%

%%\documentclass[lineno,pdflatex,sn-basic]{sn-jnl}% Basic Springer Nature Reference Style/Chemistry Reference Style

%%=========================================================================================%%
%% the documentclass is set to pdflatex as default. You can delete it if not appropriate.  %%
%%=========================================================================================%%

%%\documentclass[sn-basic]{sn-jnl}% Basic Springer Nature Reference Style/Chemistry Reference Style

%%Note: the following reference styles support Namedate and Numbered referencing. By default the style follows the most common style. To switch between the options you can add or remove Numbered in the optional parenthesis.
%%The option is available for: sn-basic.bst, sn-chicago.bst%

%%\documentclass[pdflatex,sn-nature]{sn-jnl}% Style for submissions to Nature Portfolio journals
%%\documentclass[pdflatex,sn-basic]{sn-jnl}% Basic Springer Nature Reference Style/Chemistry Reference Style
\documentclass[pdflatex,sn-mathphys-num]{sn-jnl}% Math and Physical Sciences Numbered Reference Style
%%\documentclass[pdflatex,sn-mathphys-ay]{sn-jnl}% Math and Physical Sciences Author Year Reference Style
%%\documentclass[pdflatex,sn-aps]{sn-jnl}% American Physical Society (APS) Reference Style
%%\documentclass[pdflatex,sn-vancouver-num]{sn-jnl}% Vancouver Numbered Reference Style
%%\documentclass[pdflatex,sn-vancouver-ay]{sn-jnl}% Vancouver Author Year Reference Style
%%\documentclass[pdflatex,sn-apa]{sn-jnl}% APA Reference Style
%%\documentclass[pdflatex,sn-chicago]{sn-jnl}% Chicago-based Humanities Reference Style

%%%% Standard Packages
%%<additional latex packages if required can be included here>

\usepackage{graphicx}%
\usepackage{multirow}%
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}%
\usepackage{mathrsfs}%
\usepackage[title]{appendix}%
\usepackage{xcolor}%
\usepackage{textcomp}%
\usepackage{manyfoot}%
\usepackage{booktabs}%
\usepackage{algorithm}%
\usepackage{algorithmicx}%
\usepackage{algpseudocode}%
\usepackage{listings}%
\usepackage{stmaryrd}
\usepackage{xspace}
\usepackage{amsmath}
\usepackage{mathpartir}
\usepackage{subcaption}
\usepackage{tcolorbox}
\usepackage{contour}

\contourlength{1pt}

%%%%

%%%%%=============================================================================%%%%
%%%%  Remarks: This template is provided to aid authors with the preparation
%%%%  of original research articles intended for submission to journals published
%%%%  by Springer Nature. The guidance has been prepared in partnership with
%%%%  production teams to conform to Springer Nature technical requirements.
%%%%  Editorial and presentation requirements differ among journal portfolios and
%%%%  research disciplines. You may find sections in this template are irrelevant
%%%%  to your work and are empowered to omit any such section if allowed by the
%%%%  journal you intend to submit to. The submission guidelines and policies
%%%%  of the journal take precedence. A detailed User Manual is available in the
%%%%  template package for technical guidance.
%%%%%=============================================================================%%%%

%% as per the requirement new theorem styles can be included as shown below
\theoremstyle{thmstyleone}%
\newtheorem{theorem}{Theorem}%  meant for continuous numbers
%%\newtheorem{theorem}{Theorem}[section]% meant for sectionwise numbers
%% optional argument [theorem] produces theorem numbering sequence instead of independent numbers for Proposition
\newtheorem{lemma}[theorem]{Lemma}%
\newtheorem{proposition}[theorem]{Proposition}%
%%\newtheorem{proposition}{Proposition}% to get separate numbers for theorem and proposition etc.

\theoremstyle{thmstyletwo}%
\newtheorem{example}{Example}%
\newtheorem{remark}{Remark}%

\theoremstyle{thmstylethree}%
\newtheorem{definition}{Definition}%

\raggedbottom
%%\unnumbered% uncomment this for unnumbered level heads

\newcommand{\gsep}{\mspace{8mu}\lvert\mspace{8mu}}

% types
\newcommand{\sBool}{\textit{Bool}}
\newcommand{\sNat}{\textit{Nat}}
\newcommand{\sNatList}{\textit{NatList}}
\newcommand{\sAlg}{\Rightarrow}
\newcommand{\sArr}{\to}
\newcommand{\sFunctorApp}[2]{#1 ~ #2} % spaced
% \newcommand{\sFunctorApp}[2]{#1(#2)} % parenthesized
\newcommand{\sMu}[1]{\mu #1}
\newcommand{\sFunctorConSig}[3]{(#1) \sArr \sFunctorApp{#2}{#3}}
\newcommand{\sCoerceSig}[2]{#1 <:: #2}
% terms
\newcommand{\sLet}[3]{\text{let}~ #1 = #2 ~\text{in}~ #3}
\newcommand{\sCoerce}[2]{\text{coerce}~ #1 ~ #2}
% coercions
\newcommand{\sCata}[2]{\text{cata}~ #1 ~ #2}
\newcommand{\sRoll}[1]{\text{roll}~ #1}
\newcommand{\sUnroll}[1]{\text{unroll}~ #1}
\newcommand{\sSubCov}[2]{\text{cov}[#1] ~ #2}
\newcommand{\sSubData}[1]{\text{id}[\sMu{#1}]}
\newcommand{\sSubArr}[2]{\text{arr}~ #1 ~ #2}
\newcommand{\sSubAlg}[2]{\text{alg}[#1] ~ #2}
% constructors
\newcommand{\sTrue}{\textit{True}}
\newcommand{\sFalse}{\textit{False}}
\newcommand{\sZero}{\textit{Zero}}
\newcommand{\sSuc}{\textit{Suc}}
\newcommand{\sNil}{\textit{Nil}}
\newcommand{\sCons}{\textit{Cons}}

% helpers
\newcommand{\freshMetavariables}[1]{#1 ~\text{fresh}}
\newcommand{\isDatatype}[1]{#1 ~\text{data}}
\newcommand{\atPos}[1]{^{#1}}
% \newcommand{\atPos}[1]{}

\newcommand{\Match}{\textbf{match }}
\newcommand{\With}{\textbf{ with}}
\newcommand{\Case}{\textbf{case }}
\newcommand{\Func}[1]{\textsc{#1}}

\newcommand{\todo}[1]{{{\contour{red}{\textbf{\color{black}TODO}}} \color{red}#1}}

% \newcommand{\todoHenry}[1]{\Colorbox{red!20}{#1}}
\newcommand{\todoHenry}[1]{%
  \begin{tcolorbox}[
      colback=red!15,     % Light red background
      colframe=red!50,    % Muted red border
      arc=0mm,            % Sharp corners
      boxrule=0.5pt,      % Border thickness
      left=6pt, right=6pt, top=6pt, bottom=6pt, % Inner padding
      before=\par\smallskip, after=\par\smallskip % Paragraph-like spacing
    ]
    % \textbf{[TODO for Henry]:}
    \todo{Henry}
    \\[1em]
    #1
  \end{tcolorbox}%
}

\newcommand{\todoAaron}[1]{%
  \begin{tcolorbox}[
      colback=blue!15,     % Light red background
      colframe=blue!50,    % Muted red border
      arc=0mm,            % Sharp corners
      boxrule=0.5pt,      % Border thickness
      left=6pt, right=6pt, top=6pt, bottom=6pt, % Inner padding
      before=\par\smallskip, after=\par\smallskip % Paragraph-like spacing
    ]
    \todo{Aaron}
    \\[1em]
    #1
  \end{tcolorbox}%
}

\include{definitions}

\begin{document}

\title{Convenient Algebraic Programming with Coercive Subtyping}

\author*[1]{\fnm{Henry} \sur{Blanchette}}
\email{blancheh@umd.edu}

\author*[2]{Aaron Stump}
\email{stumpaa@bc.edu}

\affil[1]{\orgname{University of Maryland}}
\affil[2]{\orgname{Boston College}}

% \begin{CCSXML}
%   <ccs2012>
%   <concept>
%   <concept_id>10011007.10011006.10011008.10011009.10011012</concept_id>
%   <concept_desc>Software and its engineering~Functional languages</concept_desc>
%   <concept_significance>500</concept_significance>
%   </concept>
%   <concept>
%   <concept_id>10011007.10011006.10011008.10011024.10011033</concept_id>
%   <concept_desc>Software and its engineering~Recursion</concept_desc>
%   <concept_significance>500</concept_significance>
%   </concept>
%   </ccs2012>
% \end{CCSXML}

% \ccsdesc[500]{Software and its engineering~Functional languages}
% \ccsdesc[300]{Software and its engineering~Recursion}

\keywords{strong functional programming, type-based termination, Mendler-style recursion}

\abstract{
  This paper presents a system for more convenient algebraic programming, a form of total functional programming where datatypes are defined as least fixed points of covariant signature functors, and recursive functions are defined as algebras. This approach requires the use of certain combinators to roll and unroll fixed points, and to convert algebras to functions which can be applied to recurse over data. These combinators are a burden for programming in this style. To alleviate that burden, in this paper we propose to insert them automatically, using coercive subtyping. The type checker generates subtyping constraints, which are then solved with the help of a custom logic programming engined called Chronolog. The subtyping axioms are given to Chronolog as rules, which uses them to solve the subtyping constraints. To avoid infinite applications of rules for unrolling signature functors, Chronolog provides a mechanism for suspending and resuming certain forms of goals. The subtyping axioms presented to Chronolog are an algorithmic version of the usual declarative rules for algebraic programming. We prove equivalence of the declarative and algorithmic rules, in Agda. We present examples of algebraic programs written in our system.
}

\maketitle

\section{Introduction} % Aaron

% Algebraic programming as an example of total functional programming (cite Turner)
% Mendler-style algebra
% Burden of inserting roll/unroll and cata
% We will reduce this burden using coercive subtyping to infer the coercions

Strong functional programming is a form of pure functional programming
where all functions are statically required to terminate on all
inputs.  One way to achieve this is through an algebraic approach:
datatypes are least fixed points $\mu F$ of signature functors $F$,
and recursive functions are least fixed points $\cata{a}$ of algebras
$a$.  The functions are called catamorphisms.  The signature functor
can be seen as describing one layer of the data.  The least fixed
point then describes data consisting of any finite number of layers.
We may unroll $\mu F$ to $F\ (\mu F)$, and roll it back again.  Such
transformations are needed to allow construction of data by applying
constructors of the signature functor, and destruction by
pattern-matching against those constructors.  Algebras interpret the
operations declared for one layer of the data, and catamorphisms then
apply algebras across all the layers.  There is no other mechanism for
recursion or looping in the language.  This leads to a strong
guarantee: all functions written in this style are guaranteed to
terminate.

One hindrance to algebraic programming is the need to apply the
functions for rolling and unrolling signature functors, and to apply
$\cata{-}$ to obtain a recursive function from an algebra.  In this
paper, we remove this hindrance by automatically inserting those
coercions wherever they are needed in code.  This means that the
programmer may write code that looks indistinguishable from regular
pure-functional code, and yet falls within the algebraic style, thus
assuring termination.  Code which attempts to make disallowed
recursive calls will still be rejected by the type checker.

To insert these functions automatically, we turn to the technique of
coercive subtyping.  With this approach, the type checker generates
subtyping constraints that need to hold, in order for the term to be
well-typed.  The constraints are then processed by a constraint
solver.  Successful runs of the solver compute the coercions for
the proven subtyping statements.  These are then inserted in the code,
at the places where the subtyping constraints were generated.  The
constraints may contain type variables, which should be instantiated
during solving.

To implement this, we make use of a custom logic-programming engine
called Chronolog.  The subtyping axioms are given to Chronolog as
rules.  Chronolog then uses standard SLD resolution to search for
proofs of the constraints, instantiating type variables during proof
search via unification.  The rules for rolling and unrolling
signatures would, if handled naively, lead to diverging proof search,
as functors could be unrolled unboundedly, and even rolling could
diverge, if type variables are instantiated to functor applications,
which then roll back to just a bare type variable again.  To avoid
this, Chronolog provides a mechanism to indicate that certain goals
should be suspended.  We identify syntactic forms of goals which
should not be processed, on pain of divergence.  These suspended goals
may be resumed if they are instantiated enough during solving of other
goals.  If not, an outer loop of our algorithm handles them by simply
equating the lower and upper bound of the subtyping constraints.

An important technical issue with this approach concerns the subtyping
rules themselves.  Declarative rules for subtyping generally are not
algorithmic, due to the presence of a transitivity rule.  This rules
could be applied indefinitely during proof search, as it posits an
intermediate type $B$ for solving a constraint $A <: C$, and requiring
subgoals $A <: B$ and $B <: C$.  To handle this, one must devise an
equivalent set of rules without transivity.  The goal could be
compared to cut-elimination in sequent calculus, where the cut rule is
a form of transitivity of entailment.  We identify
(Section~\ref{sec:subtyping}) an algorithmic set of subtyping rules,
without transitivity, and prove it equivalent to the declarative
rules.  This both ensures we can use logic programming (with our
algorithmic rules) to solve these subtyping constraints, and also
assures us that the algorithmic rules are indeed sound and complete.
This proof of equivalence has been formalized and checked in Agda.

To summarize the contributions:
\begin{itemize}
  \item We propose a new approach to total algebraic programming,
    where function calls for rolling and unrolling signature functors
    and turning algebras into catamorphisms are inferred by coercive
    subtyping.
  \item We realize this approach by generating subtyping constraints
    during typing, and solving them using a constraint solver.  The
    solver is a logic-programming engine, with a custom mechanism
    to suspend goals that would other lead to divergence.
  \item We propose an algorithmic version of the subtyping
    rules for this theory, and prove it equivalent to the
    declarative ones.  The proof is checked in Agda.
\end{itemize}

\section{Motivating examples}

\todoHenry{
  pick nice example

  subtraction uses unroll, but not roll or cata
  length uses roll, but not unroll or cata
  write an outer function that subtracts some quantity from the length of a list

  here it is in Haskell
  here it is in algebraic style with coercions

  Can we use the algebraic style with lighter syntax?  This would make it more
  feasible in practice.
}

% example: subtraction

% % ----
% % folded-out pred
% % ----

% \begin{figure}
% \begin{verbatim}
% pred = \a -> case a of
%   Zero -> Zero
%   Suc a' -> a'

% sub = \a b -> case b of
%   Zero -> a
%   Suc b' -> pred (sub a b')
% \end{verbatim}
%   \caption{Haskell implementation of subtraction.}
%   \label{fig:example-subtraction-haskell}
% \end{figure}

% \begin{figure}
% \begin{verbatim}
% pred = λ n . γ n .
%   { Zero() → Zero()
%   | Suc(n') → n' }

% sub = λ m . ω sub' n .
%   { Zero() → m
%   | Suc(n') → pred (sub' n') }
% \end{verbatim}
%   \caption{Algebraic implementation subtraction, without coercions.}
%   \label{fig:example-subtraction-haskell}
% \end{figure}

% \begin{figure}
% \begin{verbatim}
% pred = λ n . γ (unroll id) n .
%   { Zero() → (roll id) Zero()
%   | Suc(n') → n' }

% sub = λ m . ω sub' n . γ n .
%   { Zero() → m
%   | Suc(n') → γ pred (sub' n') .
%     { Zero → (roll id) Zero()
%     | Suc(m') -> m' } }
% \end{verbatim}
%   \caption{Algebraic implementation subtraction, with coercions.}
%   \label{fig:example-subtraction-haskell}
% \end{figure}

% % ----
% % folded-in pred
% % ----

% \begin{figure}
% \begin{verbatim}
% sub :: Nat -> Nat -> Nat
% sub m n = case n of
%   Zero -> m
%   Suc n' -> case sub m n' of
%     Zero -> Zero
%     Sub m' -> m'
% \end{verbatim}
%   \caption{Haskell implementation of subtraction.}
%   \label{fig:example-subtraction-haskell}
% \end{figure}

% \begin{figure}
% \begin{verbatim}
% sub : Nat → Nat ⇒ Nat
% sub = λ m . ω sub' n .
%   { Zero() → m
%   | Suc(n') → γ sub' n' .
%     { Zero() → Zero()
%     | Suc(m') → m' } }
% \end{verbatim}
%   \caption{Algebraic implementation subtraction, without coercions.}
%   \label{fig:example-subtraction-haskell}
% \end{figure}

% \begin{figure}
% \begin{verbatim}
% sub : Nat → Nat ⇒ Nat
% sub = λ m . ω sub' n . γ n .
%   { Zero() → m
%   | Suc(n') → γ (unroll id) (sub' n') .
%     { Zero() → (roll id) Zero()
%     | Suc(m') → m' } }
% \end{verbatim}
%   \caption{Algebraic implementation subtraction, with coercions.}
%   \label{fig:example-subtraction-haskell}
% \end{figure}

% % example: length

% \begin{figure}
% \begin{verbatim}
% length :: [Nat] -> Nat
% length l = case l of
%   [] -> Zero
%   h : t -> Suc(length t)
% \end{verbatim}
%   \caption{Haskell implementation of length.}
%   \label{fig:example-length-haskell}
% \end{figure}

% \begin{figure}
% \begin{verbatim}
% length : NatList ⇒ Nat
% length = ω length' l . γ l
%   { Nil → Zero()
%   | Cons(h, t) → Suc(length' t) }
% \end{verbatim}
%   \caption{Algebraic implementation length, without coercions.}
%   \label{fig:example-length-haskell}
% \end{figure}

% \begin{figure}
% \begin{verbatim}
% length : NatList ⇒ Nat
% length = ω length' l . γ l
%   { Nil → (roll id) Zero()
%   | Cons(h, t) → (roll id) Suc((cata length') t) }
% \end{verbatim}
%   \caption{Algebraic implementation length, with coercions.}
%   \label{fig:example-length-haskell}
% \end{figure}

% % example: sum of list elements

% \begin{figure}
%   \centering
%   \begin{subfigure}[b]{0.45\textwidth}
%     \centering
%     \begin{verbatim}
% add :: Nat -> Nat -> Nat
% add m n = case n of
%   Zero -> m
%   Suc n' -> Suc(add m n)

% sum :: [Nat] -> Nat
% sum l = case l of
%   [] -> Zero
%   n : l' -> add n (sum l')
%     \end{verbatim}
%     \caption{Haskell implementation of sum.}
%     \label{fig:example-sum-haskell}
%   \end{subfigure}
%   \\ \vspace{1em}
%   \begin{subfigure}[b]{0.45\textwidth}
%     \centering
%     \begin{verbatim}
% add : Nat → Nat ⇒ Nat
% add = λ m . λ n . ω add' n .
%   { Zero() → m
%   | Suc(n') → Suc(add' n) }

% sum : NatList ⇒ Nat
% sum = ω sum' l .
%   { Nil → Zero()
%   | Cons(n, l') → add n (sum' l') }
%     \end{verbatim}
%     \caption{Algebraic implementation of sum, without coercions.}
%     \label{fig:example-sum-algebraic-without-coercions}
%   \end{subfigure}
%   \hfill
%   \begin{subfigure}[b]{0.45\textwidth}
%     \centering
%     \begin{verbatim}
% add : Nat → Nat ⇒ Nat
% add = λ m . λ n . ω add' n .
%   { Zero() → m
%   | Suc(n') → (roll id) Suc(add' n) }

% sum : NatList ⇒ Nat
% sum = ω sum' l .
%   { Nil → (roll id) Zero()
%   | Cons(n, l') → (cata add) n (sum' l') }
%     \end{verbatim}
%     \caption{Algebraic implementation of sum, with coercions.}
%     \label{fig:example-sum-algebraic-with-coercions}
%   \end{subfigure}
% \end{figure}

% % example: sub and list filter

% \begin{figure}
%   \centering
%   \begin{subfigure}[b]{0.95\textwidth}
%     \centering
%     \begin{verbatim}
% sub : Nat → Nat ⇒ Nat
% sub = λ m . ω sub' n . γ n .
%   { Zero() → m
%   | Suc(n') → γ (unroll id) (sub' n') .
%     { Zero() → (roll id) Zero()
%     | Suc(m') → m' } }

% filter : (Nat → Bool) → NatList ⇒ NatList
% filter = λ p . ω filter' l .
%   { Nil → Nil()
%   | Cons(h, t) →
%       let t' = filter' t in
%       γ p h . { True → (roll id) Cons(h, t') | False → t' } }

% nonzeros : NatList ⇒ NatList
% nonzeros = filter (λ n . γ (unroll id) n . { Zero → False | Suc(n') → True })
%     \end{verbatim}
%     \caption{Algebraic implementation of subtraction and list filter, with coercions.}
%     \label{fig:example-filter-algebraic-with-coercions}
%   \end{subfigure}
% \end{figure}

% example: subs

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.95\textwidth}
    \centering
    \todoHenry{Haskell version}
    \caption{Haskell}
    \label{fig:example-subs-haskell}
  \end{subfigure}

  \vspace{1em}

  \begin{subfigure}[b]{0.95\textwidth}
    \centering
    \todoHenry{Algebraic version without coercions}
    \caption{Algebraic}
    \label{fig:example-subs-algebraic-without-coercions}
  \end{subfigure}

  \vspace{1em}

  \begin{subfigure}[b]{0.95\textwidth}
    \centering
    \begin{verbatim}
sub : Nat → Nat ⇒ Nat
sub = λ m . ω sub'(n) . γ n .
          { Zero() → m
          | Suc(n') → γ (unroll id) (sub' n') .
              { Zero() → (roll id) Zero()
              | Suc(m') → m' } }

subs : NatList ⇒ NatList → NatList
subs = ω subs'(l1) . λ l2 . γ l1 .
          { Nil → Nil
          | Cons(h1, t1) → γ (unroll id) l2 .
              { Nil → (roll id) Nil
              | Cons(h2, t2) → (roll id) Cons((cata (sub h1)) h2, subs' t1 t2) } }
    \end{verbatim}
    \caption{Algebraic with coercions}
    \label{fig:example-subs-algebraic-with-coercions}
  \end{subfigure}

  \caption{Implementations of \texttt{sub} and \texttt{subs}.}
  \label{fig:example-subs}
\end{figure}

\section{Syntax}

\todoHenry{
  Write prose (maybe Aaron helping with prose)
}

\begin{figure}[H]
  \centering

  % [TODO from Henry]: Do I actually need to define the domains for term variables, coercion variables, etc. as sets or is there a more standard way?
  % [TODO from Henry]: Somehow we need to say that type variables can *only* appear in subtyping constraints, since we don't want to claim the system supports polymorphism
  \begin{align*}
    \begin{array}{rrcl}
      \text{(Datatype Names)} &
      D &\in& \textbf{DatatypeNames}
      \\[1em]
      %
      \text{(Term variables)} &
      x &\in& \textbf{TermVars}
      \\[1em]
      %
      \text{(Coercion variables)} &
      r &\in& \textbf{CoercionVars}
      \\[1em]
      %
      \text{(Recursion Type variables)} &
      R &\in& \textbf{RecTypeVars}
      \\[1em]
      %
      \text{(Type variables)} &
      X, Y &\in& \textbf{TypeVars}
      \\[1em]
      %
      \text{(Contexts)} &
      \Gamma &::=&
      [] \mid
      \Gamma , (x : A) \mid
      \Gamma , (R : \ast)
      \\[1em]
      %
      \text{(Types)} &
      A, B
      & ::= &
      X \mid
      R \mid
      \sMu{D} \mid
      \sFunctorApp{D}{A} \mid
      A \sArr B \mid
      A \sAlg B
      \\[1em]
      %
      \text{(Terms)} &
      a, b, f
      & ::= &
      x \mid
      D.k(\overline{a,}) \mid
      \lambda (x : A) . b \mid
      (f ~ a) \mid
      \omega f(x) . b \mid
      \\[0.5em] &&&
      \gamma a . \{ \overline{k(\overline{x,}) \mapsto b ~ |} \} \mid
      \sLet{x}{a}{b} \mid
      \sCoerce{c}{a}
      \\[1em]
      \text{(Coercions)} &
      c, c_1, c_2
      & ::= &
      r \mid
      \sCata{c_1}{c_2} \mid
      \sRoll{c} \mid
      \sUnroll{c} \mid
      \sSubCov{D}{c} \mid
      \sSubData{D} \mid
      \\[0.5em] &&&
      \sSubArr{c_1}{c_2} \mid
      \sSubAlg{D}{c}
    \end{array}
  \end{align*}
\end{figure}

\section{Typing}
\label{sec:typing}

\todoHenry{prose}

\begin{figure}[H]
  \centering

  \begin{mathpar}
    \inferrule[Gamma]{
      a : \sFunctorApp{D}{C} \qquad
      (\forall i)~ k_i : \sFunctorConSig{\overline{A_{ij},}}{\sFunctorApp{D}{C}} \qquad
      (\forall i)~ \Gamma, \overline{x_{ij} : A_{ij} \vdash b_i : B_i}
    }{
      \Gamma \vdash \gamma a . \{ \overline{k_i(\overline{x_{ij}}) \mapsto b_i ~|} \} : B
    }

    \inferrule[Omega]{
      \Gamma, (R : \ast), (f : R \sArr A), (x : \sFunctorApp{D}{R}) \vdash b : B
    }{
      \Gamma \vdash \omega f(x) . b : D \sAlg B
    }

    \inferrule[Subtype]{
      \Gamma \vdash A <: A' \qquad
      \Gamma \vdash a : A
    }{
      \Gamma \vdash a : A'
    }
  \end{mathpar}
\end{figure}

\section{Subtyping} % Aaron
\label{sec:subtyping}

We begin with the declarative subtyping rules corresponding to the
coercions of Figure~\ref{fig:coercion-type-checking-rules}, and then
propose an algorithmic version, where all meta-variables in the
premises of the rule are already present in the conclusion, and the
sizes of the constraints decrease.  This ensures that SLD resolution
will not diverge on ground constraints.  We prove that the two sets
of rules are equivalent.

\subsection{Declarative subtyping}

\newcommand{\declcata}[1]{
  \inferrule[#1]{ }{
    D \sAlg A <: \sMu{D} \sArr A
}}

\begin{figure}[H]
  \centering

  \begin{mathpar}

    \inferrule[Refl]
    {
      \isDatatype{D}
    }
    {
      \sMu{D} <: \sMu{D}
    }

    \inferrule[Arr]
    {
      A' <: A \quad
      B <: B'
    }
    {
      A \sArr B <: A' \sArr B'
    }

    \inferrule[Cov]{
      A <: A'
    }{
      \sFunctorApp{D}{A} <: \sFunctorApp{D}{A'}
    }

    \inferrule[Alg]{
      A <: A'
    }{
      D \sAlg A <: D \sAlg A'
    }

    \inferrule[Roll]{ }{
      \sFunctorApp{D}{\sMu{D}} <: \sMu{D}
    }

    \inferrule[Unroll]{ }{
      \sMu{D} <: \sFunctorApp{D}{\sMu{D}}
    }

    \declcata{Cata}

    \inferrule[Trans]{
      A <: A' \quad
      A' < A''
    }{
      A <: A''
    }
  \end{mathpar}

  \caption{Declarative subtyping rules.}
  \label{fig:declarative-subtyping-rules}
\end{figure}

Figure~\ref{fig:declarative-subtyping-rules} shows the rules for declarative
subtyping $A <: B$.  We have reflexivity rule \textsc{Refl} for datatypes.  \textsc{Arr} is the usual
subtyping rule for function types, which is contravariant in the
domain and covariant in the codomain.  \textsc{Cov} expresses that the
signature functor is indeed a (covariant) functor.  \textsc{Alg}
expresses that algebra types $D \sAlg A$ are covariant in their
carriers $A$.  \textsc{Roll} and \textsc{Unroll} together express that
$\mu F$ is equivalent to $F\ (\mu F)$.  \textsc{Cata} expresses that
an algebra can be coerced to a function, namely the algebra's
catamorphism.  Finally, there is the \textsc{Trans} rule, completing
the definition of subtyping as a partial order.

\subsection{Algorithmic subtyping}

\newcommand{\decproccata}[1]{
  \inferrule[#1]{
    B <:: D \quad
    A <:: C
  }{
    D \sAlg A <:: B \sArr C
}}

\begin{figure}[H]
  \centering

  \begin{mathpar}

    \inferrule[Refl]
    {
      \isDatatype{D}
    }
    {
      \sMu{D} <:: \sMu{D}
    }

    \inferrule[Arr]
    {
      A' <:: A \quad
      B <:: B'
    }
    {
      A \sArr B <:: A' \sArr B'
    }

    \inferrule[Cov]{
      A <:: A'
    }{
      \sFunctorApp{D}{A} <:: \sFunctorApp{D}{A'}
    }

    \inferrule[Alg]{
      A <:: A'
    }{
      D \sAlg A <:: \sMu{D} \sAlg A'
    }

    \inferrule[Roll]{
      A <:: \sMu{D}
    }{
      \sFunctorApp{D}{A} <:: \sMu{D}
    }

    \inferrule[Unroll]{
      \sMu{D} <:: A
    }{
      \sMu{D} <:: \sFunctorApp{D}{A}
    }

    \decproccata{Cata}

  \end{mathpar}

  \caption{Algorithmic subtyping rules.}
  \label{fig:algorithmic-subtyping-rules}
\end{figure}

Figure~\ref{fig:algorithmic-subtyping-rules} presents the rules for
algorithmic subtyping $A <:: B$.  We use similar names as for the
declarative rules, but critically, the algorithmically problematic
\textsc{Trans} rule is not present.  A consequence of this is that now
several rules that did not have premises in the declarative
formulation, here do.  For example, compare the declarative (on the
left) and algorithmic \textsc{Cata} rules:
\[
  \begin{array}{lll}
    \declcata{\ } &\ \ \ \ \ & \decproccata{\ }
  \end{array}
\]
\noindent The algorithmic rule needs to take into account subtyping
relationships between the components in the conclusion, because there
is no rule for transitivity, which could allow those relationships to
be taken into account after this rule is applied.  In effect,
transitivity needs to be built into all the other rules.  We see a
similar change from the declarative to the algorithmic versions of the
\textsc{Roll} and \textsc{Unroll} rules, for rolling and unrolling the
signature functor.

\begin{lemma}[Decrease]
  \label{lem:decrease}
  For all rules of Figure~\ref{fig:algorithmic-subtyping-rules}, the sizes
  of the constraints in the premises are less than the size of the constraint
  in the conclusion.
\end{lemma}

\begin{theorem}[Decidability]
  \label{thm:dec}
  It is decidable whether or not two types are in the algorithmic subtyping
  relation.
\end{theorem}
\begin{proof}
  Bottom-up application of the rules to ground types is guaranteed to terminate,
  by Lemma~\ref{lem:decrease}.  Therefore, proof search may be used as a decision
  procedure for algorithmic subtyping of ground types.
\end{proof}

\subsection{Equivalence of declarative and algorithmic subtyping}
\label{sec:equiv}

While Theorem~\ref{thm:dec} ensures that we can test ground subtyping
constraints using proof search, we must confirm that the algorithmic
rules are indeed a correct version of the declarative ones.  For this,
we prove the following theorems.

\begin{theorem}[Soundness]
  $A <:: B$ implies $A <: B$.
\end{theorem}

\begin{theorem}[Completeness]
  $A <: B$ implies $A <:: B$.
\end{theorem}

The sets of rules are quite similar, so these proofs are not onerous.  They do depend on the following
inductively proven lemmas.

\begin{lemma}[roll1 (declarative)]
  If $A <: \mu D$, then $D\ A <: \mu D$.
\end{lemma}

\begin{lemma}[roll2 (declarative)]
  If $\mu D <: A$, then $\mu D <: D\ A$.
\end{lemma}

\begin{lemma}[refl (algorithmic)]
  $A <:: A$.
\end{lemma}

\begin{lemma}[trans (algorithmic)]
  If $A <:: B$ and $B <:: C$ then $A <:: C$.
\end{lemma}

\begin{lemma}[roll (algorithmic)]
  $D\ \mu D <: \mu D$
\end{lemma}

\begin{lemma}[unroll (algorithmic)]
  $\mu\ D <: D\ \mu D$
\end{lemma}

\subsection{Formalization in Agda}
\label{sec:agda}

We formalize the above development in Agda as follows.  Types are
represented by the Agda datatype \verb|Ty| in
Figure~\ref{fig:synagda}.  For simplicity, we have single signature
functor \verb|D| and its least fixed-point \verb|μD| (note that this
  is a single identifier in Agda, not an application of a \verb|μ|
operator to \verb|D|).  Algebra types $D \To A$ are represented just
as \verb|D⇒ A|.  Note that \verb|D⇒| is also a single symbol in Agda,
like \verb|μD|.  Arrow types are represented $\arr{A}{B}$ (as other
arrow notations are already in use).  The figure declares some fixity
information, for lighter notation where these operators are used
later.

\begin{figure}
\begin{verbatim}
data Tp : Set where
  D_ : Tp → Tp
  _⟶_ : Tp → Tp → Tp
  D⇒ : Tp → Tp
  μD : Tp

infixr 9 _⟶_
infixr 10 D_
\end{verbatim}
  \caption{Agda definition of types}
  \label{fig:synagda}
\end{figure}

Figures~\ref{fig:declagda} and~\ref{fig:algagda} show the
representations of the declarative and algorithmic subtyping systems,
respectively.  We follow a standard encoding strategy, where each set
of rules is represented as an indexed inductive type.  Each rule
becomes a constructor of the type.  For example, we have, for both
types, \verb|Refl| of type \verb|μD <: μD|, which corresponds exactly
to the \verb|Refl| rules of
Figures~\ref{fig:declarative-subtyping-rules}
and~\ref{fig:algorithmic-subtyping-rules}.  Conveniently, Agda allows
reusing constructor names across datatypes.  Dependent typing
is used for quantification.  For example, the \verb|Cata| constructor
for declarative subtyping (in Figure~\ref{fig:declagda})
has type
\begin{verbatim}
{X : Tp} → D⇒ X <: μD ⟶ X
\end{verbatim}
\noindent This is a dependent function type, stating that give
\verb|X : Tp|, the \verb|Cata| constructor will return a proof of
\verb|D⇒ X <: μD ⟶ X|, expressing the coercion from an algebra with
carrier \verb|X| to its catamorphism.  Finally, the statements of
soundness and completeness are shown in Figure~\ref{fig:sndcmpl}.  We
omit the proofs of these and the lemmas listed in
Section~\ref{sec:equiv}.

The total number of lines for all the Agda code is under 150.  This
shows that the proof burden of the approach is very manageable.  Part
of the reason for this is that the types do not contain bound
variables, and so the technical overhead associated with reasoning in
the presence of those does not arise.

\begin{figure}
\begin{verbatim}
infix 5 _<:_

data _<:_ : Tp → Tp → Set where
  Refl : μD <: μD
  Arr : {T1 T2 T1' T2' : Tp} →
         T1' <: T1 →
         T2 <: T2' →
         T1 ⟶ T2 <: T1' ⟶ T2'
  Roll : D μD <: μD
  Unroll : μD <: D μD
  Cov : {T1 T2 : Tp} → T1 <: T2 → D T1 <: D T2
  Trans : {T1 T2 T3 : Tp} → T1 <: T2 → T2 <: T3 → T1 <: T3
  Cata : {X : Tp} → D⇒ X <: μD ⟶ X
  Alg : {T1 T2 : Tp} → T1 <: T2 → D⇒ T1 <: D⇒ T2
\end{verbatim}
  \caption{Agda definition of declarative subtyping}
  \label{fig:declagda}
\end{figure}

\begin{figure}
\begin{verbatim}
infix 5 _<::_

data _<::_ : Tp → Tp → Set where
  Refl : μD <:: μD
  Arr : {T1 T2 T1' T2' : Tp} →
         T1' <:: T1 →
         T2 <:: T2' →
         T1 ⟶ T2 <:: T1' ⟶ T2'
  Roll : {T : Tp} → T <:: μD → D T <:: μD
  Unroll : {T : Tp} → μD <:: T → μD <:: D T
  Cov : {T1 T2 : Tp} → T1 <:: T2 → D T1 <:: D T2
  Alg : {T1 T2 : Tp} → T1 <:: T2 → D⇒ T1 <:: D⇒ T2
  Cata : {T T1 T2 : Tp} →
        T1 <:: μD →
        T <:: T2 →
        D⇒ T <:: T1 ⟶ T2
\end{verbatim}
  \caption{Agda definition of algorithmic subtyping}
  \label{fig:algagda}
\end{figure}

\begin{figure}
\begin{verbatim}
Soundness    : {T T' : Tp} → T <:: T' → T <: T'
Completeness : {T T' : Tp} → T <: T'  → T <:: T'
\end{verbatim}
  \caption{Agda statements of soundness and completeness}
  \label{fig:algagda}
\end{figure}

\section{Typing checking with constraints}
\label{sec:typechecking}

We present a type checking procedure that uses a logic-programming engine to solve subtyping constraints.
First, subtyping constraints are generated from a structurally recursive analysis of a term, where each constraint corresponds to a position in the term at which a coercion (which may end up being the identity) is required to justify the subtype relation.
Then, these subtyping constraints are solved by a logic-programming engine by applying the algorithmic subtyping rules in Figure \ref{fig:algorithmic-subtyping-rules}, where each rule corresponds to a constructor of the coercion to be inserted at the constraint's associated position in the term.

\begin{figure}[H]
  \centering

  \begin{mathpar}

    \inferrule[Var]{
      (x : A) \in \Gamma
    }{
      \Gamma \vdash x : A \gsep \varnothing \implies \varnothing
    }

    \inferrule[Con]{
      % [Note] we need to freshen X here since the constructor rules dont deal with type variables
      \freshMetavariables{X} \qquad
      k : \sFunctorConSig{\overline{B_i,}}{D}{X} \qquad
      (\forall i)~ \Gamma \vdash a\atPos{p_i}_i : A_i \gsep \mathcal{A}_{a_i} \implies \mathcal{C}_{a_i}
    }{
      \Gamma \vdash k(\overline{a_i,}) : \sFunctorApp{D}{X} \gsep \bigcup_i (\{ A_i <::\atPos{p_i} B_i \} \cup \mathcal{A}_{a_i}) \implies \bigcup_{i} \mathcal{C}_{a_i}
    }

    \inferrule[Lam]{
      \freshMetavariables{X} \qquad
      \Gamma, (x : X) \vdash b : B \gsep \mathcal{A}_b \implies \mathcal{C}_b
    }{
      \Gamma \vdash \lambda x . b : X \sArr B \gsep \mathcal{A}_b \implies \mathcal{C}_b
    }

    \inferrule[App]{
      % [Note]: We introduce a type variable for the supertype of the codomain of f so that B <:: A -> X is allowed, and then any subtyping required to make A fit is done on f rather than a
      \freshMetavariables{X} \qquad
      \Gamma \vdash f : B \gsep \mathcal{A}_f \implies \mathcal{C}_f \qquad
      \Gamma \vdash a : A \gsep \mathcal{A}_a \implies \mathcal{C}_a
    }{
      \Gamma \vdash f ~ a : X \gsep
      \{ B <:: A \sArr X \} \cup \mathcal{A}_f \cup \mathcal{A}_a \implies \mathcal{C}_f \cup \mathcal{C}_a
    }

    \inferrule[Gamma]{
      % [Note]: C is the type of the thing being discriminated, which should be a subtype of D(X), where X is the type variable for the recursive parameter of D, which could be refined by C <:: D(X). And Y is the type variable for the output of the entire pattern match, which is refined by B_i <:: Y for the outputs of each branch which are the B_i
      \freshMetavariables{X, Y} \qquad
      \Gamma \vdash a : C \gsep \mathcal{A}_a \implies \mathcal{C}_a
      \\\\
      % [TODO]: enforce exhaustive pattern match
      (\forall i)~ k_i : \sFunctorConSig{\overline{A_{ij}},}{D}{X} \qquad
      \qquad
      (\forall i)~ \Gamma, \overline{x_{ij} : A_{ij},} \vdash b_i : B_i \gsep
      \mathcal{A}_{b_i} \implies \mathcal{C}_{b_i}
    }{
      \Gamma \vdash \gamma a . \{ \overline{k_i(\overline{x_{ij},}) \mapsto b_i ~ |} \} : Y
      \gsep
      \mathcal{A}_a \cup \bigcup_i \mathcal{A}_{b_i}
      \implies
      \{ C <:: \sFunctorApp{D}{X} \} \cup
      \mathcal{C}_a \cup
      \bigcup_i (\{ B_i <:: Y \} \cup \mathcal{C}_{b_i})
    }

    % [Bookmark] This is where the axiom about omega type R is introduced.
    \inferrule[Omega]{
      % [TODO from Henry]: need to enforce that R and r are linked to this *appearance* of \omega. I could add position annotations to all terms and subtype constraints, but that's quite verbose, so I should probably just say this in prose somewhere.
      % [TODO]: is is a problem that r is introduced fresh here even though its not a type variable?
      \freshMetavariables{R, r} \qquad
      \Gamma, (R : \ast), (r : \sCoerceSig{R}{R}), (f : R \sArr A), (x : \sFunctorApp{D}{R}) \vdash b : B \gsep \mathcal{A}_b \implies \mathcal{C}_b
    }{
      \Gamma \vdash \omega f(x) . b : D \sAlg B \gsep \mathcal{A}_b \cup \{ R <:: R \} \implies \mathcal{C}_b
    }

    \inferrule[Let]{
      \Gamma \vdash a : A \gsep \mathcal{A}_a \implies \mathcal{C}_a \qquad
      \Gamma, (x : A) \vdash b : B \gsep \mathcal{A}_b \implies \mathcal{C}_b
    }{
      \Gamma \vdash \sLet{x}{a}{b} : B \gsep
      \mathcal{A}_a \cup \mathcal{A}_b \implies \mathcal{C}_a \cup \mathcal{C}_a
    }

    % [Note from Henry]: Actually this is unneeded in the subtype constraint generation rules, since coercions should only appear in solved terms.
    % % [Note from Henry]: since this is already a coercion, requires that the domain *exactly* matches the inferred type of the arg
    % \inferrule[InferCoe]{
    %   \Gamma \vdash c : \sCoerceSig{A}{B} \qquad
    %   \Gamma \vdash a : A \gsep \mathcal{A}_a \implies \mathcal{C}_a
    % }{
    %   \Gamma \vdash \sCoerce{c}{a} : B \gsep \mathcal{A}_a \implies \mathcal{C}_a
    % }

  \end{mathpar}

  \caption{Constraint generation rules.}
  \label{fig:constraint-generation-rules}
\end{figure}

\begin{figure}[H]
  \centering

  \begin{mathpar}

    \inferrule[VarCoe]{
      (r : \sCoerceSig{A}{B}) \in \Gamma
    }{
      \Gamma \vdash r : \sCoerceSig{A}{B}
    }

    \inferrule[ReflCoe]{
      \isDatatype{D}
    }{
      \Gamma \vdash \sSubData{D} : \sCoerceSig{\sMu{D}}{\sMu{D}}
    }

    \inferrule[ArrCoe]{
      \Gamma \vdash c_1 : \sCoerceSig{A'}{A} \qquad
      \Gamma \vdash c_2 : \sCoerceSig{B}{B'}
    }{
      \Gamma \vdash \sSubArr{c_1}{c_2} : \sCoerceSig{A \sArr B}{A' \sArr B'}
    }

    \inferrule[CovCoe]{
      \Gamma \vdash c : \sCoerceSig{A}{A'}
    }{
      \Gamma \vdash \sSubCov{D}{c} : \sCoerceSig{\sFunctorApp{D}{A}}{\sFunctorApp{D}{A'}}
    }

    \inferrule[AlgCoe]{
      \Gamma \vdash c : \sCoerceSig{A}{A'}
    }{
      \Gamma \vdash \sSubAlg{D}{c} : \sCoerceSig{D \sAlg A}{D \sAlg A'}
    }

    \inferrule[RollCoe]{
      \isDatatype{D} \qquad
      \Gamma \vdash c : \sCoerceSig{A}{\sMu{D}}
    }{
      \Gamma \vdash \sRoll{c} : \sCoerceSig{\sFunctorApp{D}{A}}{\sMu{D}}
    }

    \inferrule[UnrollCoe]{
      \isDatatype{D} \qquad
      \Gamma \vdash c : \sCoerceSig{\sMu{D}}{A}
    }{
      \Gamma \vdash \sUnroll{c} : \sCoerceSig{\sFunctorApp{D}{A}}{\sMu{D}}
    }

    \inferrule[CataCoe]{
      \Gamma \vdash c_1 : \sCoerceSig{B}{D} \qquad
      \Gamma \vdash c_2 : \sCoerceSig{A}{C}
    }{
      \Gamma \vdash \sCata{c_1}{c_2} : \sCoerceSig{D \sAlg A}{B \sArr C}
    }
  \end{mathpar}

  \caption{Coercion type checking rules.}
  \label{fig:coercion-type-checking-rules}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{mathpar}
    % Bool

    \inferrule[DataBool]{ }{\isDatatype{\sBool}}

    \inferrule[Bool.True]{ }{
      \sTrue : \sFunctorConSig{}{\sBool}{A}
    }

    \inferrule[Bool.False]{ }{
      \sFalse : \sFunctorConSig{}{\sBool}{A}
    }

    % Nat

    \inferrule[DataNat]{ }{\isDatatype{\sNat}}

    \inferrule[Nat.Zero]{ }{
      \sZero : \sFunctorConSig{}{\sNat}{A}
    }

    \inferrule[Nat.Suc]{ }{
      \sSuc : \sFunctorConSig{A}{\sNat}{A}
    }

    % NatList

    \inferrule[DataNatList]{ }{\isDatatype{\sNatList}}

    \inferrule[NatList.Nil]{ }{
      \sNil : \sFunctorConSig{}{\sNatList}{A}
    }

    \inferrule[NatList.Cons]{ }{
      \sCons : \sFunctorConSig{\sNat, A}{\sNatList}{A}
    }
  \end{mathpar}

  \caption{Datatype constructor type inference rules.}
  \label{fig:datatype-contructor-type-inference-rules}
\end{figure}

\subsection{Constraint generation}

Constraint generation for a term $a$ in context $\Gamma$ is performed by applying the constraint generation rules in Figure \ref{fig:constraint-generation-rules} recursively on the structure of $a$, which upon success produces a derivation of $\Gamma \vdash a : A \mid \mathcal{A} \implies \mathcal{C}$.
This is a usual typing judgment augmented with a constraint problem $\mathcal{A} \implies \mathcal{C}$, where $\mathcal{A}$ is a set of axioms and $\mathcal{C}$ is a set of goals.
Each constraint in these sets is of the form $A <::^p B$, where $p$ is a position of a sub-term of $a$.
The system is designed such that a solution to $\mathcal{A} \implies \mathcal{C}$ specifies an insertion of coercions around sub-terms of $a$, where each coercion inserted at a position $p$ corresponds to one of the original required constraints $A <::^p B$.

The constraint generation phase also performs type inference, however in contrast to usual type inference and checking procedures, this procedure cannot decide a term to be mal-typed.
The constraint generation procedure will generate a constraint problem for any well-formed and well-scoped $\Gamma, a, A$.
A derivation $\Gamma \vdash a : A \mid \mathcal{A} \implies \mathcal{C}$ merely states that $a$ has type $A$ \textit{only if} the constraint problem $\mathcal{A} \implies \mathcal{C}$ is solvable.

For example, constraint generation for \texttt{sub : Nat → Nat ⇒ Nat} results in the following constraint problem, where $X,Y,Z,W,U$ are fresh type variables and $R$ is a fresh recursion type variable:

% [TODO]: Make sure to update this if the constraint generation rules change.
% [TODO]: insert term positions on constraints
\begin{mathpar}
  \{
    R <:: R
  \}
  \implies
  \{
    % X is domain of λ m ...
    % R is the recursive type var for omega
    % Y is the output type of sub' n
    % U is return of γ (and so is return of ω, and so is return of λ)
    % W is the recursive param of Nat for the pattern match
    % Z is the recursive param of Zero()
    %
    % γ ...
    X \sArr \sNat \sAlg U <:: \sMu\sNat \sArr \sNat \sAlg \sMu\sNat,
    % γ sub' n' ...
    \sFunctorApp{\sNat}{Y} <:: \sFunctorApp{\sNat}{W},
    % sub' n
    R \sArr \sMu\sNat <:: R \sArr \sFunctorApp{\sNat}{Y},
    % roll Zero()
    \sFunctorApp{\sNat}{Z} <:: U,
    % m'
    \sMu\sNat <:: U
  \}
\end{mathpar}

Here is where the axioms and requirements are introduced.
\begin{itemize}
  \item The axiom $R <:: R$ is introduced by $\omega ~ sub'(n)$.
  \item The requirement $X \sArr \sNat \sAlg U <:: \sMu\sNat \sArr \sNat \sAlg \sMu\sNat$ is introduced by $\gamma ~ n$.
  \item \todo{explain sources of other requirements}
\end{itemize}

\todoHenry{Prose}

\subsection{Constraint solving with Chronolog}

Once a constraint problem $\mathcal{A} \implies \mathcal{C}$ has been generated for a term, we apply a logic-programming approach to solving these constraints which computes the coercions that justify the original subtyping relations.
Our approach is based on a novel logic-programming engine, Chronolog, which adopts an alternative technique for goal suspension compared to rule-level techniques in Prolog variants (e.g. Prolog II).
% [TODO]: source
Goal suspension is critical in this context due to the presence of the \textsc{Roll} and \textsc{Unroll} rules, which can be composed infinitely by straightforward SLD resolution.
Chronolog prevents this by supporting a global suspension meta-predicate, $S$, where goals that satisfy $S$ are immediately suspended before being processed further.
Suspended goals are still refined by logic variable substitutions.
If such a refinement results in a goal no longer satisfying $S$, then the goal is resumed.
% In our implementation, we use this suspension meta-predicate:
For our implementation, we use a meta-predicate for $S$ defined by the following rules.

\begin{mathpar}
  \inferrule[]{X ~\text{is a logic variable}}{S(X <:: \sMu{D})}

  \inferrule[]{X ~\text{is a logic variable}}{S(X <:: \sFunctorApp{D}{A})}

  \inferrule[]{X ~\text{is a logic variable}}{S(X <:: R)}

  \inferrule[]{X ~\text{is a logic variable}}{S(\sMu{D}) <:: X}

  \inferrule[]{X ~\text{is a logic variable}}{S(\sFunctorApp{D}{A}) <:: X}

  \inferrule[]{X ~\text{is a logic variable}}{S(R) <:: X}

  \inferrule[]{X,Y ~\text{are logic variables}}{S(X <:: Y)}
\end{mathpar}

These rules are designed to prevent Chronolog from entering an infinite regress where a cycle of rules can be applied infinitely.
% [TODO]: argue for why the rules assure this

Our implementation uses Chronolog inside an outer loop that specially refines just one of the suspended goals according to \textsc{Refine} as defined below. \todo{fully explain how this works}

\begin{mathpar}
  \begin{array}{rlcrll}
    \textsc{Refine}(\sMu{D} &<:: X) &=& \textsc{Refine}(X &<:: \sMu{D}) &= \{ X \mapsto \sMu{D} \} \\
    \textsc{Refine}(\sFunctorApp{D}{A} &<:: X) &=& \textsc{Refine}(X &<:: \sFunctorApp{D}{A}) &= \{ X \mapsto \sFunctorApp{D}{Y} \} ~\text{where}~ Y ~\text{fresh} \\
    \textsc{Refine}(R &<:: X) &=& \textsc{Refine}(X &<:: R) &= \{ X \mapsto R \} \\
    &&& \textsc{Refine}(X &<:: Y) &= \{ X \mapsto Y \}
  \end{array}
\end{mathpar}

Chronolog can terminate in three ways: (1) failure with unsolved goals, (2) progress with suspended goals, (3) success with no suspended goals.
Altogether, our constraint solving procedure is the following:

\begin{algorithm}
  \caption{Constraint solving with Chronolog.}
  \label{alg:constraint-solving}
  \begin{algorithmic}[1]
    \Function{Solve}{$\mathit{axioms}, \mathit{goals}$}
    \State $\sigma \gets \emptyset$ % \Comment{Initialize as the empty logic variable substitution}
    \While{$\mathit{goals} \neq \emptyset$}
    \State $\mathit{result} \gets \Func{Chronolog}(\mathit{axioms}, \mathit{goals}, \sigma$)
    \Statex
    % Pattern-matching block
    \State \Match $\mathit{result}$ \With
    \State \quad \Case $\langle \text{``failure''} \rangle \Rightarrow$
    \State \qquad \Return $\bot$
    \Statex
    \State \quad \Case $\langle \text{``success''}, \sigma_{\mathit{new}} \rangle \Rightarrow$
    \Return $\sigma_{\mathit{new}}$
    \Statex
    \State \quad \Case $\langle \text{``progress''}, \sigma', \mathit{goals}' \rangle \Rightarrow$
    \State \qquad $\sigma'' \gets \Func{Refine}(\sigma', \mathit{goals}')$
    \State \qquad $\mathit{goals} \gets \sigma''(\mathit{goals}')$
    \EndWhile
    \EndFunction
  \end{algorithmic}
\end{algorithm}

% The rules for rolling and unrolling
% signatures would, if handled naively, lead to diverging proof search,
% as functors could be unrolled unboundedly, and even rolling could
% diverge, if type variables are instantiated to functor applications,
% which then roll back to just a bare type variable again.  To avoid
% this, Chronolog provides a mechanism to indicate that certain goals
% should be suspended.  We identify syntactic forms of goals which
% should not be processed, on pain of divergence.  These suspended goals
% may be resumed if they are instantiated enough during solving of other
% goals.  If not, an outer loop of our algorithm handles them by simply
% equating the lower and upper bound of the subtyping constraints.

\todoHenry{Prose}

% \subsection{Implementation with Chronolog} % Henry
% \todoHenry{Prose}

\section{Returning to the Examples} % Henry

\todoHenry{
  Prose
  remind the version with coercions
  show without coercions
}

\section{Related Work} % Aaron

Turner introduced the term \emph{strong functional programming},
presented several arguments in favor of the approach, and proposed an
\emph{elementary} way to realize such a language, in the form of
static checks for structural recursion~\cite{turner95}.  Mendler
proposed a recursor using an abstract type to ensure that recursive
calls are permitted only on subdata for an inductive
type~\cite{mendler91}.  The algebraic approach has been applied for
modular implementation of interpreters~\cite{swierstra08}.

Charity is a strong functional programming language based on
categorical abstractions for initial algebras and final
coalgebras~\citep{charity92}. Tofu uses the Calculus of Constructions
as a language for defining terminating functions, again based on
categorical ideas~\citep{tofu10}.  Charity and Tofu both support
coinductive datatypes, which we did not study here.

\section{Conclusion and future work} % Aaron

We have seen how total algebraic programming can be made more feasible
by inferring the basic coercions necessary for the method.  These are
for rolling and unrolling signature functors, and for turning an
algebra into its catamorphism.  We considered an approach to this
problem, where type inference generates subtyping constraints, whose
solutions correspond to the needed coercions.  Our constraint solver
uses a logic-programming engine to process constraints with respect to
an algorithmic version of the subtyping rules.  This algorithmic
subtyping relation was proved equivalent to the declarative one, in
Agda.

Future work includes extending this paradigm further, to richer forms
of algebraic programming.  One form that has been proposed, as a
powerful generalization of the basic structural recursion captured by
Mendler algebras, is that of recursive coalgebras~\cite{capretta+06}.
This formulation encompasses divide-and-conquer algorithms, which are
beyond the scope of structural recursion.  A different approach to
divide-and-conquer recursion has also been considered
by~\citet{abreu+23}.  That approach requires algebras to make various
function calls to pass between various abstract types.  These would be
excellent candidates for inference using coercive subtyping, as we
proposed in this paper.  It would also be interesting to infer
coercions for coalgebraic programming with coinductive types.

% Richer patterns of recursion, as in POPL 2023 paper

%\bibliographystyle{plain}
\bibliography{paper.bib}

\end{document}
